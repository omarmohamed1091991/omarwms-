# 🔒 إعداد سياسات RLS لعزل رسائل المستخدمين

## المشكلة التي تم حلها
جميع الرسائل كانت تظهر لجميع المستخدمين بدلاً من أن يرى كل مستخدم رسائله فقط.

## الحل
تم تحديث ملف `scripts/fix_incoming_messages_rls.sql` لتقييد الوصول حسب `user_id`.

## خطوات التطبيق

### 1. تشغيل السكريبت في Supabase
1. افتح Supabase Dashboard
2. اذهب إلى **SQL Editor**
3. انسخ محتوى `scripts/fix_incoming_messages_rls.sql`
4. الصقه في SQL Editor
5. اضغط **Run** لتشغيل السكريبت

### 2. التحقق من السياسات الجديدة
بعد تشغيل السكريبت، يجب أن تكون السياسات التالية موجودة:

**SELECT (القراءة):**
- المستخدمون العاديون: يرون رسائلهم فقط (`user_id = auth.uid()`)
- الأدمن: يرى جميع الرسائل (بغض النظر عن `user_id`)

**INSERT (الإضافة):**
- Webhooks تستطيع إضافة رسائل (service role يتجاوز RLS)

**UPDATE (التحديث):**
- المستخدمون: يحدثون رسائلهم فقط
- الأدمن: يحدث جميع الرسائل

**DELETE (الحذف):**
- المستخدمون: يحذفون رسائلهم فقط
- الأدمن: يحذف جميع الرسائل

### 3. اختبار النظام

**اختبار 1: المستخدم العادي**
1. سجل دخول كمستخدم عادي (مثل: omar1@gmail.com)
2. اذهب لصندوق الوارد
3. يجب أن ترى رسائلك فقط التي وصلت لرقم WhatsApp الخاص بك

**اختبار 2: الأدمن**
1. سجل دخول كأدمن (admin@rosesmile.com)
2. اذهب لصفحة المستخدمين
3. ادخل على صندوق وارد أي مستخدم
4. يجب أن ترى جميع رسائل ذلك المستخدم

## كيف يعمل النظام

### استقبال الرسائل عبر Webhook
```
Meta WhatsApp → /api/webhooks/[userId] → حفظ في incoming_messages مع user_id
```

كل webhook له URL خاص بالمستخدم:
- المستخدم 1: `/api/webhooks/123-user-id-1`
- المستخدم 2: `/api/webhooks/456-user-id-2`

### عرض الرسائل
- **المستخدم العادي**: `SELECT * FROM incoming_messages WHERE user_id = auth.uid()`
- **الأدمن**: `SELECT * FROM incoming_messages` (بدون تقييد)

## ملاحظات مهمة

- Webhook يستخدم `SUPABASE_SERVICE_ROLE_KEY` لذلك يتجاوز RLS ويستطيع إضافة الرسائل
- كل مستخدم له webhook URL خاص يحتوي على user_id
- RLS policies تضمن أن كل مستخدم يرى رسائله فقط
- الأدمن له صلاحيات كاملة لإدارة جميع الرسائل

## استكشاف الأخطاء

**المشكلة**: لا تزال أرى رسائل مستخدمين آخرين
**الحل**: تأكد من تشغيل السكريبت في Supabase SQL Editor

**المشكلة**: لا أرى أي رسائل
**الحل**: 
1. تحقق من أن webhook URL صحيح في إعدادات WhatsApp
2. تحقق من أن `user_id` في URL يطابق user_id في قاعدة البيانات

**المشكلة**: الرسائل لا تصل
**الحل**: المشكلة ليست في RLS، بل في webhook configuration
