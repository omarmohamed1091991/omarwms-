# 🧪 كيفية اختبار النظام

## اختبار التوزيع الاحترافي للرسائل

### السيناريو 1: عميل يرسل لمستخدم واحد

1. افتح WhatsApp من هاتف العميل
2. أرسل رسالة إلى رقم **omar** (623846684149569)
3. انتقل إلى الموقع وسجل دخول كـ **omar**
4. ✅ يجب أن تظهر الرسالة في صندوق وارد omar فقط

### السيناريو 2: نفس العميل يرسل لمستخدم آخر

1. من نفس هاتف العميل
2. أرسل رسالة إلى رقم **ايه** (913640735159381)
3. انتقل إلى الموقع وسجل دخول كـ **ايه**
4. ✅ يجب أن تظهر الرسالة في صندوق وارد ايه فقط
5. ✅ يجب ألا تظهر في صندوق وارد omar

### السيناريو 3: عميلين مختلفين

1. **العميل الأول** يرسل إلى رقم omar
2. **العميل الثاني** يرسل إلى رقم ايه
3. ✅ كل رسالة تذهب للصندوق الصحيح

---

## 📊 التحقق من البيانات

### في Supabase SQL Editor:

```sql
-- عرض توزيع الرسائل
SELECT 
  up.full_name as مستخدم,
  up.whatsapp_phone_number_id as رقم_الواتساب,
  COUNT(DISTINCT im.sender_phone) as عدد_العملاء,
  COUNT(im.id) as عدد_الرسائل,
  STRING_AGG(DISTINCT im.sender_phone, ', ') as أرقام_العملاء
FROM user_profiles up
LEFT JOIN incoming_messages im ON im.user_id = up.id
WHERE up.role != 'admin' OR up.whatsapp_phone_number_id IS NOT NULL
GROUP BY up.id, up.full_name, up.whatsapp_phone_number_id
ORDER BY up.full_name;
```

---

## ✅ النتائج المتوقعة

| المستخدم | رقم الواتساب | عدد الرسائل | ملاحظات |
|---------|--------------|-------------|---------|
| omar | 623846684149569 | X رسالة | من عملائه فقط |
| ايه | 913640735159381 | Y رسالة | من عملائها فقط |

**كل مستخدم يرى رسائله الخاصة فقط ✅**
