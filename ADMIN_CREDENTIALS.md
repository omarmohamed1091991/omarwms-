# بيانات تسجيل الدخول للأدمن

## حساب المدير العام (Admin)

**البريد الإلكتروني:** admin@rosesmile.com  
**كلمة المرور:** Admin@12345

---

## الصلاحيات

### أدمن (Admin):
- الوصول إلى صفحة إدارة المستخدمين (`/users`)
- إنشاء وحذف وتعديل المستخدمين
- تغيير صلاحيات المستخدمين
- الوصول إلى جميع البيانات

### مستخدم عادي (User):
- الوصول فقط إلى لوحة التحكم الخاصة به (`/users/{userId}`)
- إدارة رسائله الخاصة فقط
- لا يمكنه رؤية بيانات المستخدمين الآخرين

---

## كيفية إنشاء حساب الأدمن

1. قم بتشغيل السكريبت SQL في Supabase:
   ```sql
   -- الموجود في scripts/add_user_roles.sql
   ```

2. بعد ذلك، قم بتسجيل الدخول باستخدام البيانات أعلاه

3. إذا لم يتم إنشاء الحساب تلقائياً، يمكنك إنشاؤه يدوياً من لوحة Supabase

---

## ملاحظات مهمة

- يجب تغيير كلمة المرور الافتراضية بعد أول تسجيل دخول
- يمكن إنشاء أكثر من حساب أدمن من خلال تغيير دور المستخدم إلى "admin"
- جميع المستخدمين الجدد يتم إنشاؤهم بدور "user" افتراضياً
